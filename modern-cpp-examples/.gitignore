# macOS 编译产物
#
# 每个 .cpp 会生成一个同名的、无扩展名的可执行文件（Windows 版是 .exe，已被根目录 .gitignore 的 *.exe 覆盖），
# 加 -g 编译时还会额外生成同名的 .dSYM/ 调试符号目录。两者都与源文件同目录，这里统一忽略。
#
# 写法说明：先忽略整个子目录，再反向放行 .cpp。
# 以后新增 cpp2x 目录时照着加两行即可（忽略目录 + 放行 .cpp）。
cpp11/*
cpp14/*
cpp17/*
cpp20/*
cpp23/*
!cpp11/*.cpp
!cpp14/*.cpp
!cpp17/*.cpp
!cpp20/*.cpp
!cpp23/*.cpp
