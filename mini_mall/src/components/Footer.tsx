export function Footer() {
  return (
    <footer className="mt-12 border-t py-6">
      <div className="mx-auto flex w-full max-w-6xl flex-col items-center gap-1 px-4 text-sm text-muted-foreground">
        <p>mini_mall 微型电商 — 学习项目（Next.js 16 + Prisma 7 + better-auth）</p>
        <p>本商店为演示用途，支付为模拟支付，不产生真实交易</p>
      </div>
    </footer>
  );
}
