# demo_pkg/__init__.py —— 包标记文件
# 这是什么: __init__.py —— 目录成为"包"(可被 import)的标记; 也为空时
#           import demo_pkg 即成功。内容可写"导入时执行的初始化代码"
#           (如定义 __all__、便捷导入), 但最常见的形态就是空文件。
# 本文件与 demo_pkg/greet.py 是 02_packages_venv.py 的演示脚手架,
# 不属于 34 个交付示例文件。
